
  <br><br><br><br><br>
  <div class="container">
    <div class="row">
      <div class="col-lg-12">
        
        <h1>Notes</h1>
        <p class="top_border">
        </p>
        <br>

        <p id="notice"><%= flash[:search_result_notice] %></p>
        
        <table border="1">
          <tr>
            <th>+</th>
            <th>Name</th>
            <th>Production</th>
            <th>Role</th>
            <th>Instruments</th>
          </tr>
         
          <% @search_result.each do |note| %>
            <tr>
              <td><%= link_to "+", new_note_path(:id => note.id), :style=> "color: #2C3E50" %></td>
              <td><%= link_to note.name, profile_path(note.profile_id), :style=> "color: #2C3E50k" %></td>
              <td><%= note.production.name %></td>
              <td><%= note.role %></td>
              <td><%= note.get_instruments %></td>
            </tr>
          <% end %>
        </table>

        <br>  
       
      </div>
    </div>
  </div>

