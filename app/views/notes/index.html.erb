<h1>Listing notes</h1>
 
<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
  </tr>
 
  <% @notes.each do |note| %>
    <tr>
      <td><%= note.name %></td>
      <td><%= note.description%></td>
      <td><%= link_to 'Show', note_path(note) %></td>
    </tr>
  <% end %>
</table>